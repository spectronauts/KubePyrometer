apiVersion: v1
kind: Service
metadata:
  name: net-echo
  labels:
    app: net-echo
    kb-step: "{{.step}}"
    kb-type: net-echo
spec:
  selector:
    app: net-echo
    kb-step: "{{.step}}"
  ports:
    - port: 8080
      targetPort: 8080
